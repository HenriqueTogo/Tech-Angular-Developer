"use strict";
function addNumber(x, y) {
    return x + y;
}
function addHello(name) {
    return "Hello " + name;
}
let soma = addNumber(4, 7);
let hello = addHello("Henrique");
console.log(soma);
console.log(hello);
